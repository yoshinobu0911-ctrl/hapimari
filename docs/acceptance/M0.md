# M0: 基盤 — 受け入れ記録

実施日: 2026-07-05 / 実施者: Claude Code（AIエージェント）

## 受け入れ条件（SPEC §6 M0）

> `pnpm dev` でmobile/adminが起動し、seedスクリプトでダミーユーザー20名（男12女8、東京・埼玉・千葉）が投入される。

## 結果: ✅ 合格

### 1. `pnpm dev` で mobile / admin が起動する

```
• Packages in scope: @hapimari/admin, @hapimari/mobile, @hapimari/shared
@hapimari/admin:dev: ▲ Next.js 16.2.10 (Turbopack)
@hapimari/admin:dev: - Local: http://localhost:3000
@hapimari/admin:dev: ✓ Ready in 2.6s
@hapimari/mobile:dev: Starting project at C:\Users\haosh\dev\hapimari\apps\mobile
```

- `http://localhost:3000` → HTTP 200（Next.js admin）
- `http://localhost:8081/status` → `packager-status:running`（Expo Metro）

### 2. Seedデータ投入（`pnpm db:reset` / 初回 `supabase start` で自動投入）

DB検証クエリの結果（`docker exec supabase_db_hapimari psql ...`）:

| 検証項目 | 期待値 | 実測 | 判定 |
|---|---|---|---|
| 男性 / 女性 | 12 / 8 | 12 / 8 | ✅ |
| 都県分布 | 東京・埼玉・千葉のみ | 東京8・埼玉7・千葉5 | ✅ |
| 35〜45歳バツイチ子持ち女性 | 5名 | 5名 | ✅ |
| 全員 is_verified=true | 20名 | 20名 | ✅ |
| マッチ済みペア | 2組 | 2組 | ✅ |
| うち1組 message_count=22 | 22（トリガで自動計上） | 22 / call_unlocked=true | ✅ |

### 3. 追加検証（スキーマ・制約・RLS）

- **R1 年齢制約（DB check）**: 34歳女性・44歳男性のINSERTが `profiles_min_age_check` 違反で拒否されることを確認 ✅
- **RLS**: 全9テーブル（profiles / verifications / likes / matches / messages / date_proposals / reports / calls / daily_stats）で `relrowsecurity = true` ✅
- **message_countトリガ**: seedのメッセージ26通挿入で matches.message_count が 22 / 4 に自動計上 ✅
- **型生成**: `supabase gen types typescript --local` → `packages/shared/src/types/database.ts`（573行）✅
- **Lint**: `pnpm exec biome check .` エラー0 ✅
- **テスト**: Vitest 18件成功（fraud_words 50語・隣接県マップ対称性・R1年齢判定）✅
- **CI**: `.github/workflows/ci.yml`（pnpm install → lint → test）を配置 ✅

## 手動確認手順（人間向け）

```powershell
cd C:\Users\haosh\dev\hapimari
pnpm install
pnpm db:start     # Docker Desktop 起動が前提
pnpm dev          # admin: http://localhost:3000 / mobile: ターミナルのQRコード
```

- Supabase Studio: http://localhost:54323 → Table Editor で profiles 20行を確認
- seedユーザーのログイン: `seed01@hapimari.test` 〜 `seed20@hapimari.test` / パスワード `password123`
  （01〜12が男性、13〜20が女性。13=ようこ は 01=たかし とマッチ済み・22通）

## 特記事項

1. **リポジトリの場所**: `C:\Users\haosh\dev\hapimari`。
   当初 cowork directory（OneDrive配下・日本語パス）に作成したが、Supabase CLI が日本語パスでサイレントに失敗する問題（exit 0 で何も生成しない）のため移設した。詳細は cowork directory の `hapimari-の場所.md` を参照。
2. **verifications の RLS**: SPEC の「閲覧・更新は service_role のみ」に従い、本人でも自分の申請行をSELECTできない。M2の審査待ち画面の実装方針に影響する可能性があるため `QUESTIONS.md` に記載予定（非ブロッカー）。
3. Expo SDK 57 / Next.js 16 / Biome 2 / pnpm 11 と、いずれも新しいメジャーバージョンで構築。アプリコード実装時は各テンプレート同梱の `AGENTS.md`（バージョン差異への注意書き）に従う。
