# M3 実装指示書（Opus 4.8 用）

> **使い方**: 新しい Claude Code セッションを開き、モデルを Opus 4.8 にして（`/model claude-opus-4-8`）、
> 下の区切り線から下を**全文コピペ**して送る。それだけで実装が始まる。

---

C:\Users\haosh\dev\hapimari で作業してください。

あなたはこのプロジェクトのリードエンジニアです。まずリポジトリ直下の CLAUDE.md（開発憲法）を読み、全ルールを遵守してください。

# タスク: マイルストーン M3「検索・いいね・マッチ・メッセージ」の実装

## 進め方
1. `docs/design/M3_design.md` を読む。特に §1「前提と環境」は、この環境で実際に発生したトラブルの回避策なので実装前に必読。
2. 実装順序は設計書 §9（P1→P9）に従う。各Phase完了ごとに日本語で論理単位コミット。
3. 設計書と SPEC.md が矛盾する場合は設計書を優先する。
4. スコープは設計書 §2.1 のみ。§2.2 の Out of Scope（デート打診・通話・課金・既読管理など）を混ぜない。
5. 設計書に書かれていない設計判断が必要になったら、勝手に決めずに停止して「選択肢＋推奨案」を提示する（開発憲法の承認ゲート）。
6. 開始前チェック: Docker Desktop 起動 → `supabase start` → PATH補正（CLAUDE.md §3）。

## オーナー承認済みの設計判断（2026-07-06 確定。再確認不要・そのまま実装する）
詳細は `docs/decisions/2026-07-06_M3設計判断.md`。
- R4 いいね上限は **100件/日**（SPECの20件から変更済み）。方式は「拒否せず表示繰越」（設計書 §5.4）。
  `packages/shared/src/constants.ts` の `FEMALE_DAILY_LIKE_LIMIT` を **20→100 に更新**すること。
- blocks テーブルの新設（SPEC §3に無い追加）は承認済み。
- 通報3件以上の警告フラグは、DBカラム追加ではなく管理画面の強調表示で代替（設計書 §6.1）。

## 完了条件（すべて満たしたら停止して報告）
- 設計書 §2.3 の受け入れ条件3点がE2Eで確認できている
- 設計書 §8.4 の成果物: `docs/acceptance/M3.md` 作成 / `QUESTIONS.md` 追記 / Biome 0エラー / tsc 0エラー（mobile・admin両方）/ Vitest 全緑
- `/selfreview` を実行し、結果を `docs/review/` に保存済み

## 報告フォーマット（非エンジニアのオーナー向け・結論ファースト）
1. 変更サマリ（専門用語には1行説明）
2. オーナーが自分で試せる動作確認手順（正常系＋エラーになるべき異常系）
3. 残リスク・暫定判断
4. 業務委託エンジニアにレビューしてほしい観点（優先度順）
