# PR #1 レビュー対応 修正指示書（2026-09-20 AIレビュー・全22件）

> **この文書の位置づけ**: 別セッション／別エージェントが、この指摘対応を単独で完遂するための作業指示書。
> 原文は [`2026-09-20_AIレビュー原文.md`](2026-09-20_AIレビュー原文.md)（22件全文）。本書は仕分けと作業手順。
> 作成: 2026-09-20 / 作成者: Claude（調査・仕分けのみ。**コード修正は未着手**）

---

## 0. 着手前に必ず読むこと

### 0-1. このレビューの性質（重要）

レビュアー satoman0703 さんのコメント本文:

> これは一旦AIにレビュー走らせたもので、修正の際の参考にしてください。夜に詳しく自分でもみます👀

つまり**今回の22件はAIレビューの結果**であり、**satomanさん本人の詳細レビューが後から追加される予定**。

→ **着手前に必ず新着レビューを確認すること**:
```
gh api repos/yoshinobu0911-ctrl/hapimari/pulls/1/reviews --paginate --jq '.[] | select(.submitted_at > "2026-09-20T01:23:36Z") | {id, user: .user.login, submitted_at}'
gh api repos/yoshinobu0911-ctrl/hapimari/pulls/1/comments --paginate --jq '.[] | select(.created_at > "2026-09-20T01:23:36Z") | {id, user: .user.login, path, body: (.body[0:200])}'
```
新着があれば、本書の仕分けに追加してから作業計画を立て直す（二度手間を避けるため）。

### 0-2. 作業ツリーの状態（重要）

- **レビュー基点コミット = `d3d0711` = 現在のHEAD**。レビュアーはコミット済みの状態を見ている
- ただし**作業ツリーには2026-09-09の未コミット変更が残っている**（1週間以上放置）:
  - 変更: `apps/admin/.env.example` / `apps/admin/app/photos/page.tsx` / `apps/admin/lib/photo-ai.ts` / `packages/shared/src/index.ts` / `supabase/functions/.env.example` / `supabase/functions/like/index.ts`
  - 新規: `packages/shared/src/abuse_words.ts` / `packages/shared/test/abuse_words.test.ts` / `docs/decisions/2026-09-09_写真AI併用と暴言フィルタ承認.md`
  - 内容: 写真AIモデレーション＋いいね暴言フィルタ（2026-09-09オーナー承認済み・コード完成・実キー未検証）
- **影響**: 指摘 #22（`.env.example` に `PHOTO_MODERATION_API_KEY` が無い）は**この未コミット分で既に修正済み**。コミットすれば解消する
- **最初にやること**: この未コミット分をどう扱うかオーナーに確認し、**先にコミットして土台を固めてから**レビュー対応に入る。混ぜると差分が読めなくなる

### 0-3. 守るルール

`CLAUDE.md`（開発憲法）に従う。特に:

- **§4 提案先行**: リスク「高」は**オーナーの明示承認まで実装しない**。リスク「低」かつ1〜2ファイルの軽微な修正のみ提案と実装をまとめてよい
- **§5 最小変更**: 承認された範囲だけ。ついでのリファクタ・依存追加を混ぜない
- **§6 センシティブ領域**（自動的にリスク高）: 認証・RLS・個人情報・写真/書類・メッセージ・マッチング/いいね・通報ブロック・本人確認・**決済**・管理画面・本番設定
- **§7 禁止**: 失敗テストの黙殺、承認なしのスキーマ変更、「できていない」を隠した完了報告
- **§3 Edge Function応答規約**: 失敗は `{ ok: false, error, message }`。`message` はそのまま表示できる日本語。**画面側で文言を自作しない**

---

## 1. 全22件の仕分け表

| # | ID | 種別 | 指摘の要点 | 対象 | リスク | フェーズ |
|---|---|---|---|---|---|---|
| 1 | 4055523941 | must | `run_sql_tests.sh` が `ERROR` を数えず、アサーション0件でも「全PASS」と出る | `scripts/run_sql_tests.sh` | 低 | **P1** |
| 2 | 4055523946 | must | `test_m66` T3-a/T3-b が恒真。三点測位対策の証明になっていない | `scripts/test_m66_audit_fixes.sql` | 低 | **P1** |
| 3 | 4055523971 | sug | `count(*) >= 0` の恒真アサーション（`test_review2_fixes.sql:282-285` も同型） | `scripts/test_m67_retention.sql` ほか | 低 | **P1** |
| 4 | 4055523978 | sug | `exception when others` で無関係な失敗もPASS（T1-b/T1-cも同型） | `scripts/test_m66_audit_fixes.sql` | 低 | **P1** |
| 5 | 4055523975 | sug | T6に照合アサーションが無い（許可リスト不在） | `scripts/test_review2_fixes.sql` | 低 | **P1** |
| 6 | 4055523936 | must | `stripe_events` に処理済みフラグが無く、取りこぼすと永久未処理 | `supabase/functions/stripe-webhook/` + migration | **高（決済）** | **P2** |
| 7 | 4055523940 | must | イベント配送順が逆転すると `past_due` で固定される | `supabase/functions/stripe-webhook/` + migration | **高（決済）** | **P2** |
| 8 | 4055523950 | must | `messages.tsx` に `limit` が無く `max_rows=1000` で打ち切られ、プレビューが消える | `apps/mobile/src/app/(tabs)/messages.tsx` | 中 | **P2** |
| 9 | 4055523953 | must | agora-tokenの日本語 `message` を捨てて固定文言にしている（§3違反） | `apps/mobile/src/lib/call-provider-agora.ts` ほか | 中 | **P2** |
| 10 | 4055523986 | sug | 本人確認ゲートだけ `validateLike` の外にあり、テスト0件 | `supabase/functions/like/index.ts` + `packages/shared/src/like_rules.ts` | **高（本人確認）** | **P2** |
| 11 | 4055523959 | ask | `storage.objects` の読取ポリシーが旧 `is_photo_visible_to` のまま。写真可視判定の関数が3世代並存 | migration群 | **高（RLS）** | **P3** |
| 12 | 4055523994 | sug | `is_caller_active()` が行数ぶん実行される。スカラサブクエリで1回に | migration（新規） | 中（スキーマ） | **P3** |
| 13 | 4055524006 | sug | `photo_reviews(user_id)` に索引が無く退会処理が毎回seq scan | migration（新規） | 中（スキーマ） | **P3** |
| 14 | 4055524014 | sug | `age` は計算列なので `gte('age')` で索引の第3列が効かない | `apps/mobile/src/lib/discover-query.ts` ± migration | 中 | **P3** |
| 15 | 4055523990 | sug | 受信いいねを全件取得してから `IN` に投げている（`.limit()` 不足） | `apps/mobile/src/app/(tabs)/likes.tsx` | 中 | **P3** |
| 16 | 4055524024 | sug | Realtime購読に `filter` が無く、全マッチの更新で一覧が丸ごと再取得 | `apps/mobile/src/app/(tabs)/messages.tsx` | 中 | **P3** |
| 17 | 4055524018 | sug | 共通処理7つが3ファイルに重複。`_shared/http.ts` へ切り出し | `supabase/functions/` 3ファイル | 中（リファクタ） | **P4** |
| 18 | 4055524027 | imo | `timingSafeEqual`/Basic解析が `admin-auth.ts` と `middleware.ts` に二重 | `apps/admin/` 2ファイル | 中（認証） | **P4** |
| 19 | 4055524015 | sug | SPEC.md の R9/R5 が実装と食い違い（モック課金・本人確認ゲート未記載） | `SPEC.md` | 低（文書） | **P4** |
| 20 | 4055524031 | nits | 棚卸し表 #37 が「意図的見送り」のまま（`server-only` は `0596540` で導入済み） | `docs/review/2026-09-02_指摘棚卸し.md` | 低（文書） | **P4** |
| 21 | 4055524034 | nits | `.env.example` に `PHOTO_MODERATION_API_KEY` が無い | `apps/admin/.env.example` | 低 | **済※** |
| 22 | 4055523963 | ask | 法定3ページのリンク先は正式文面に差し替わっているか | オーナー確認事項 | — | **P0** |

※ #21 は §0-2 の未コミット分で既に修正済み。コミットで解消（要確認のうえ報告）。

**種別の意味**: must=必須 / ask=質問 / sug=suggestion=提案 / imo=意見 / nits=軽微

---

## 2. 作業フェーズと順序

**この順序を守ること。** 特にP1を最初にやる理由は §2-1 のとおり。

### P0. 着手前（オーナーへの確認・作業0）

以下2点をオーナーに確認してから着手する。

1. **未コミット分（§0-2）を先にコミットしてよいか**（推奨: コミットしてから開始）
2. **#22 法定3ページ（特商法・利用規約・プライバシーポリシー）は正式文面に差し替わっているか**
   - 現状の記録では仮文面のプレースホルダのまま（`docs/legal/age_verification_description.md:58`）
   - 掲載先はBYYコーポレートサイト／LPリポジトリ（`C:\Users\haosh\homepage\hapimari-lp`）＝**本リポジトリ外**
   - コード修正ではなく公開前チェック項目。`docs/launch_checklist.md` に反映する

### P1. テスト基盤の信頼回復（#1〜#5・リスク低・承認不要レベル）

**最初にやる。理由**: `docs/review/2026-09-02_指摘棚卸し.md` は30件の「対応済み」の根拠に `run_sql_tests.sh` の緑を使っている。その緑が「アサーション0件でも緑」なら、**過去の対応済み判定そのものの確度が落ちる**。ここを直さないと、以降どの修正を検証しても「本当に効いているか」を言えない。

| # | やること |
|---|---|
| #1 | `run_sql_tests.sh` に (a) psqlの終了コード判定 (b) `ERROR` 行数を `FAIL_TOTAL` に加算 (c) スイートごとの期待PASS件数との突合 を入れる |
| #2 | `test_m66` T3-a/T3-b を、被害者を**生座標のまま**置き、本番の `get_profile_distances` 越しに観測する形に書き換える（現状は snap 済み座標を手写しの case 式で評価しており恒真） |
| #3 | `count(*) >= 0` の恒真アサーションを実数比較にする。匿名化検証は**対象ユーザーにメッセージを1件入れてから**消えることを確かめる |
| #4 | `exception when others` を `sqlerrm` 突合または `42501`（RLS違反）判定に変える（`test_review2_fixes.sql:33-37` が良い手本） |
| #5 | T6 に「許可リスト外で `has_function_privilege('authenticated', ...)` が真になる件数 = 0」のアサーションを1行足す |

**P1完了時の必須確認**: 修正後のスクリプトで**わざと1件失敗させて赤が出ること**を実演し、その出力を報告に貼る（緑だけ見せない）。

### P2. must の本体対応（#6〜#10・**リスク高を含む＝要提案承認**）

`CLAUDE.md §4` に従い、**実装前に設計提案を出してオーナー承認を得る**。特に #6/#7（決済）と #10（本人確認）はセンシティブ領域。

| # | 指摘 | 修正方針（レビュアー提案） | 注意 |
|---|---|---|---|
| #6 | webhook取りこぼしで永久未処理 | `stripe_events` に `processed_at timestamptz` を追加。重複キー(23505)時は `processed_at is null` なら再処理、成功時に立てる | **スキーマ変更＋決済。migration追加と型再生成がセット**。既存行のバックフィル方針も提案に含める |
| #7 | 古いイベントが後着すると `past_due` で固定 | `event.created` を保持し、反映済みより古いイベントは捨てる | #6と同じmigrationにまとめるか分けるかを提案で示す |
| #8 | メッセージ一覧のプレビューが消える | マッチごとに `limit 1` を引くRPC、または `matches` に最新メッセージを持たせる | 索引 `idx_messages_match_created` は既にある。**RPC案とスキーマ案で選択肢A/Bを出す** |
| #9 | 通話エラーの日本語messageが捨てられる | `error.context` を読んで `message` を画面に通す。`apps/mobile/src/lib/like-api.ts:29-47` が正しい手本 | あわせて `chat/[matchId].tsx:209` の通話ボタン判定が**自分の `is_verified` しか見ていない**点も報告（相手未確認時に押せてしまう） |
| #10 | 本人確認ゲートがshared外・テスト0件 | `LikeRuleUser` に `isVerified` を足し、`validateLike` に統合。`like_rules.test.ts` にテスト追加 | 迂回経路は無い（`likes` にINSERTポリシー無し）ため**緊急度は低いが、年齢確認に関わる分岐がテスト0件**という位置づけ |

### P3. 性能・整合性（#11〜#16・スキーマ変更を含む＝要提案承認）

| # | やること |
|---|---|
| #11 | **まず調査**: `is_photo_visible_to` / `is_photo_of_profile` / 3世代目の関数の現状を棚卸しし、`storage.objects` ポリシーをどれに寄せるか提案。実害の有無も併せて回答（レビュアーは「実害までは追えなかった」としている）。**RLS＝リスク高。調査結果を出してから実装** |
| #12 | `is_caller_active()` を `(select public.is_caller_active())` に包む（レビュアーがsuggestion形式で具体案を提示）。新規migrationで実施 |
| #13 | `create index idx_photo_reviews_user_id on public.photo_reviews (user_id);` を新規migrationで追加 |
| #14 | `age` での絞り込みを `birth_date` 範囲に変えるか、式インデックスを足すか。**選択肢A/Bで提案** |
| #15 | `likes.tsx` の受信いいね取得に `.limit()` を足す（索引 `idx_likes_to_user_created` あり）。繰越件数表示のズレにも触れる |
| #16 | `messages.tsx` のRealtime購読に `filter` を付ける（`likes.tsx:87` の `to_user=eq.` が手本）。`matches` UPDATE購読と二重発火する点もあわせて整理 |

**共通注意**: migrationを追加したら**必ず適用＋型再生成をセットで**行う（`CLAUDE.md §3`。「migrationを書いたのに適用し忘れる」事故の前例あり）。
```
pnpm exec supabase migration up
pnpm exec supabase gen types typescript --local | Out-File -Encoding utf8NoBOM packages\shared\src\types\database.ts
```
あわせて `supabase/schema.generated.sql` のスナップショットも更新する（レビュー導線として2026-09-02から手動運用）。

### P4. 重複解消・文書整合（#17〜#20）

| # | やること |
|---|---|
| #17 | Edge Function共通処理を `supabase/functions/_shared/http.ts` に切り出し、3ファイルから import。**既に差異が出ている**（`_shared` 版 `authenticate` は `{id, email}`、agora-token版は `{id}`）ので、統合時にどちらに寄せるか明示 |
| #18 | `timingSafeEqual`/Basic解析を `admin-auth.ts` から export し `middleware.ts` で import。**認証コードなので慎重に**。統合後、`ADMIN_PASSWORD` 未設定で503、誤パスワードで401、正パスワードで通過、の3パターンを実測する |
| #19 | `SPEC.md` の R9（「MVPはモック課金」→Stripe本実装・`is_subscription_active(auth.uid())` 判定）と R5（通話・デートは本人確認完了が前提）を実装に合わせて更新 |
| #20 | `docs/review/2026-09-02_指摘棚卸し.md` の #37 を「対応済み（`0596540`）」に更新 |

---

## 3. 検証手順（各フェーズ共通・完了報告に結果を貼ること）

```bash
# 1. 静的チェック
pnpm run lint                                   # biome 0件であること
pnpm -F @hapimari/shared exec tsc --noEmit      # 0件
pnpm -F @hapimari/mobile exec tsc --noEmit      # 0件
pnpm -F @hapimari/admin exec tsc --noEmit       # 0件

# 2. 単体テスト（2026-09-20時点のベースライン: 93件PASS）
pnpm -F @hapimari/shared test

# 3. DBテスト（要 supabase start）
npx supabase start
bash scripts/run_sql_tests.sh
```

**P1完了後は「わざと壊して赤が出るか」も必ず実演する。** 緑の報告だけでは #1 の指摘に応えたことにならない。

---

## 4. 完了報告に含めること（`CLAUDE.md §9`）

1. 対応した指摘IDと、やったこと（1行ずつ）
2. **対応しなかった指摘と、その理由**（見送りも立派な判断。隠さない）
3. 検証結果（上記コマンドの実出力。未検証項目は「未検証」と明記）
4. 残リスクと、**公開前に委託エンジニアに必ず見てほしい点**
5. `progress.md` / `tasks.md` / `docs/review/` の更新

### GitHubへの返信について

各指摘への返信コメント投稿は**オーナーの指示があってから**行う（前回2026-09-03は投稿前に本人確認を取っている）。返信文のドラフトは `docs/review/` に日付入りで用意し、投稿可否を仰ぐ。

---

## 5. やってはいけないこと

- **テストを緑にするためにアサーションを緩める**（今回の指摘の大半が「緑だが何も検証していない」問題。同じ轍を踏まない）
- 未コミット分（§0-2）とレビュー対応を**同じコミットに混ぜる**
- 承認なしのスキーマ変更・decisions未記録の設計判断
- 「実キーが無いので未検証」を伏せたまま完了と報告する（Stripe/Agora/OpenAIの実キーは未設定のまま）
