# M7.2 受け入れ記録 — 決済の画面側

> 実装: 2026-08-19（Claude / Fable 5）。設計書: `docs/design/M7_2_payment_ui_design.md`（同日オーナー承認）。
> 状態: **実装完了・検証は一部未実施**。実際の決済フロー（§B）は Stripe テストキーが
> 未設定（`supabase/functions/.env` 不在）のため実行できていない。手順は設計書 §12。

## A. 実施済みの検証（2026-08-19・実測）

| 項目 | 結果 |
|---|---|
| `pnpm -F @hapimari/shared test` | ✅ **88件成功**（新規 subscription-view.test.ts 17件を含む。従来72件→旧モックプラン検証1件を削除） |
| `pnpm exec biome check .` | ✅ 135ファイル・エラー0 |
| `tsc --noEmit`（mobile） | ✅ 0エラー |
| `tsc --noEmit`（admin） | ✅ 0エラー |
| マイグレーション適用 | ✅ `migration up` で M7.1（20260811100000）と M7.2（20260819100000）を適用。型を再生成し `subscriptions` / `stripe_events` / RPC が型に反映 |
| 照会ガード（設計 §9-5） | ✅ ローカルDBで実測: 本人としての `is_subscription_active(自分)` = true ／ `(他人)` = **false（行が存在していても）** ／ サーバー側（auth.uid()なし）= true ／ 同期トリガも従来どおり動作 |
| RLS・列GRANT | ✅ 実測: authenticated から `subscriptions` は自分の行のみ・`stripe_customer_id` の SELECT は permission denied |
| 受け入れ条件 §11-(1)(2)(3)(6)(16) の実装 | ✅ コード上実装（3→6→1の順・初期選択3ヶ月・おすすめBadge・月額/総額/間隔併記・法定4行・女性のマイページ行非表示）。画面の実描画確認は §A2 |

## A2. 画面の実描画確認（2026-08-19・Expo Web + シードユーザーで実測）

ローカルAuth APIでセッションを発行してブラウザに注入する方式でログインし
（パスワードの画面入力はしない）、以下を実際に描画・操作して確認した。
契約状態はローカルDBへの直接書き込みで再現（Webhookと同じ service_role 経路・トリガ連動も確認）。

| 状態・操作 | 結果 |
|---|---|
| 未契約の男性のプラン選択 | ✅ 3ヶ月→6ヶ月→1ヶ月の順・初期選択3ヶ月・おすすめBadge・月あたり＋総額＋自動更新間隔・法定4行（§11-1,2,3） |
| プランの選択切り替え | ✅ タップで選択が移動（ラジオ・枠色・背景） |
| 決済ボタンのエラー経路 | ✅ Stripeキー未設定の500でサーバーの日本語message「エラーが発生しました。…」が表示され、ボタンが復帰 |
| 契約中（active） | ✅ 「ご利用中: 3ヶ月プラン」「次回更新日: 2026年11月7日」（JST整形）・月あたり/総額・解約ボタン（§11-5） |
| 解約予約済み | ✅ 「2026年11月7日で終了します」＋「解約を取り消す」（§11-6） |
| 支払いトラブル（past_due） | ✅ 警告バナー＋再登録用プラン選択（設計§3.4）。トリガで subscription_active も自動で false に落ちることを確認 |
| チャットのペイウォール | ✅ 未課金男性に「プランを見る」導線。**契約付与後の再読込で送信欄が開放**（§11-18相当の表示面） |
| マイページ（男性・契約中） | ✅ 「有料プラン｜次回更新 2026年11月7日」 |
| マイページ（女性） | ✅ 有料プランの行が出ない（§11-16） |
| 女性が/subscriptionへ直接到達 | ✅ 「女性は現在、無料でご利用いただけます」のみ表示 |
| マッチ成立の演出（同日追加分） | ✅ いいね返し→like関数→実マッチ成立→演出モーダル表示→「メッセージを送る」でトークへ遷移（E2E） |

※ 実際のStripe決済（Checkoutページ・Webhook反映・ポーリング）は §B のとおり未実施のまま。

既知の対象外: 設計 §11-(4)（法定表示リンク）はオーナー判断で保留（BYYサイト掲載方針）。
既存問題: `packages/shared` 単体の `tsc --noEmit` は `compatibility.ts(128)` で従来から失敗する
（7月のコミット時点から存在・M7.2と無関係。修正は1行だが未承認のため触っていない）。

## B. 未実施（Stripe テストキー設定後に実施すること）

設計書 §11 の (7)〜(15)・(18)。実施手順は §12。特に:

- テストカード `4242 4242 4242 4242` での決済 → 戻り → ポーリングで契約中表示（8・9）
- 解約 → 解約予約表示 → 取り消し（10・11）※ シードの `seed_cus_*` 行では不可。実決済で作った契約で行う
- 契約中の再決済が `already_subscribed` で止まる（12）
- past_due からの再登録で旧契約が Stripe 側でキャンセルされる（§9-1。Stripe CLI の
  `stripe trigger` か、テストクロックで再現）
- 決済成功後にチャットの送信欄が開く（18）

## C. 委託エンジニアレビュー（(C) 本番公開前）で必ず見てほしい点

1. `stripe-checkout` の旧契約キャンセル（REPLACEABLE_STATUSES）— お金に触る唯一の追加コード
2. `stripe-webhook` の巻き戻し防止ガード（設計 §15-1）— イベント順序の競合への対処が妥当か
3. `is_subscription_active` の本人限定ガードが既存の呼び出し元（RLS・トリガ・cron）を壊していないか
4. 画面が権利判定をしていないこと（URLクエリ不信・DB行からの導出のみ）
