# PR #1 インラインコメントへの返信文ドラフト（2026-09-26 夜間作成・**未投稿**）

> **状態: ドラフト。投稿していない。** 2026-09-24 決定（運用判断 #2）により、オーナーが内容を確認してから投稿する。
> 対象: satoman0703 さんの AI レビュー（2026-09-20・22件）のうち、2026-09-26 夜間作業で**対応が完了した**ものへの返信。未完了（I12/I13/I14/I16）には「方針と時期」を返す。
> **前提: 下記のコミットはまだローカルだけにある。投稿は push の後に行うこと**（コミットIDのリンクが切れるため）。
> 検証の再現: `bash scripts/run_sql_tests.sh`（2026-09-26 時点 10スイート・計168件）／`pnpm -F @hapimari/shared test`（121件）

---

### → コメント 4055523950（メッセージ一覧の最新プレビュー欠落・#8）

ご指摘のとおり、全マッチのメッセージを1回で取得してから先頭を選んでいたため、PostgREST の `max_rows=1000` で無言に打ち切られ、古い会話のプレビューが消えていました。`78d11cb` で、マッチごとに最新1件を DB 側で選ぶ RPC `get_my_latest_messages()` に置き換えました。

- SECURITY INVOKER（既存の messages/matches の RLS がそのまま効く）＋関数内で当事者に限定。anon・PUBLIC からは実行権を剥奪
- 検証: `scripts/test_i08_latest_messages.sql`（11件）。他の会話に1100件ある状態でも各会話の最新が返ること、他人のマッチは取れないこと、RLS を素通りする service_role でも0行になること。REST でも旧方式が1000行で打ち切られ新 RPC は全マッチを返すことを実測しました

### → コメント 4055523953（通話の日本語エラー案内を捨てている・#9）

ご指摘のとおりでした。`75c9c36` で、`agora-token` の失敗応答の `message` を通話画面まで運んで表示するようにしました（規約外の本文は表示しない判定を shared の純粋関数にしてテスト）。相手が本人確認前のときは通話ボタン自体を出さないようにしています。

- 検証: supabase-js 経由で 404/410/403/400/500 の各応答から案内文を取り出せること、通信断では従来の汎用文言になることを実測。ブラウザでの表示確認はこれからです

### → コメント 4055523986（いいねの本人確認ゲートがテスト対象外・#10）

`657939f` で、本人確認の判定を `validateLikeSender()` として `like_rules.ts` に移し、`validateLike` の先頭でも判定するようにしました。Edge Function は従来と同じ位置（相手の取得より前）で呼んでいるため、HTTP の応答は変わりません。

- 検証: Vitest に4件追加。like 関数の HTTP 応答8パターンを変更前後で実測し完全一致を確認

### → コメント 4055523959（写真の閲覧者 active 検査・#11）

ご指摘のとおり、Storage 側に閲覧者の在籍条件が無く、退会・凍結・プロフィール未作成の利用者が「一覧取得→署名URL」で他人の承認済み写真を読めました。`c7f6048` で、`is_photo_visible_to`・`is_photo_of_profile` と Storage の読み取りポリシーの両方に `is_caller_active()` を追加しました（本人フォルダの未承認写真プレビューは維持）。

- 検証: `scripts/test_p2_photo_viewer_active.sql`（21件。修正前は10件が失敗することを確認済み）。Storage API を HTTP で実測し、凍結・退会・未作成の利用者は sign/get/info/list/copy がすべて拒否されることを確認
- 発行済みの署名URL（最長60分）の即時失効は別課題としています

### → コメント 4055523990（受信いいね1000件超の欠落・#15）

`202eb89` で、表示日の割当（1日100件の繰越）を DB 側で全件に対して行い、新しい順にページ化して返す RPC `get_received_likes_page()` に置き換えました（limit を足すだけの対処はしていません）。画面は50件ずつ「さらに表示」で読み込みます。

- 検証: `scripts/test_i15_received_likes_page.sql`（13件）。女性受信者の繰越 100/150、日をまたぐ繰越、ブロック・退会した送り主を割当の前に除外、1100件へのページ到達（重複なし・新しい順）

### → コメント 4055524018（Edge Function 間の HTTP ヘルパ重複・#17）

`1d91393` で `supabase/functions/_shared/http.ts` に一本化しました。`authenticate` の戻り値は `{ id, email }` 版に寄せています（決済側が email を使うため）。`_shared/stripe.ts` から再公開しているので、決済関数の import は変えていません。5関数の基本応答を変更前後で実測し、完全一致を確認しました。

### → コメント 4055524027（Basic 認証の二重実装・#18）

`a219601` で、`next/headers` に依存しない純粋ヘルパ `apps/admin/lib/basic-auth.ts` に分離し、middleware と Server Action の両方から使うようにしました（middleware が admin-auth.ts を直接 import する形は採っていません）。未設定503・誤り401・正しい200を実測しています。あわせて、本番で `x-forwarded-proto` が欠けた要求も拒否するようにしました（`ae25fce`）。

### → コメント 4055524014（年齢条件で索引が使えない・#14）

方針は「年齢の範囲を DB 側で生年月日の範囲に変換して絞る（生年月日は返さない）」で確定しています。これには検索を RPC に移す必要があり、距離の絞り込み（I30）と同じ作り直しになるため、まとめて行う方向で検討中です（設計: `docs/design/2026-09-26_I14_設計提案.md`）。

### → コメント 4055523994 / 4055524006 / 4055524024（#12 is_caller_active の再実行・#13 photo_reviews の索引・#16 Realtime 購読）

いずれも方針は承認済みですが、会員数が増えた段階で実測してから最小の変更を入れる予定です（現在の会員規模では計測に意味のある差が出ないため）。

---

## オーナー向けメモ（投稿前に確認してほしいこと）

1. **push が先**: 上記コミットはローカルだけにある。push してから投稿する。
2. 決済（I06 など）については、独立レビューで must 4件が見つかっている（`docs/review/2026-09-26_決済実装レビュー_8a77fe5.md`）。決済のコメントへの返信は、その修正方針が決まってからにするのを推奨。
3. Grok・Codex・Sonnet のレビューは GitHub のコメントではないため、返信の対象外。
