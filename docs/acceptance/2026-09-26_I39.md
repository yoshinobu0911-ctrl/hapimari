# 受け入れ記録: I39 通話記録（calls）の INSERT に本人確認条件を追加（2026-09-26 夜間作業）

- 実装: Claude Opus 5.5（夜間自律作業・モードA）
- 設計: [I39 設計提案](../design/2026-09-26_I39_設計提案.md)（夜間指示書 §1 により推奨案で自己承認）
- 人間レビューの段階: (B) MVP 完成時。**RLS 変更**

## 対象

| ファイル | 内容 |
|---|---|
| `supabase/migrations/20260926140000_i39_calls_insert_verified.sql`（新規） | calls の INSERT ポリシーを「当事者・active・本人確認済み・非ブロックのみ記録作成可」に作り直し（本人行の `is_verified` を追加） |
| `scripts/test_i39_calls_insert_verified.sql`（新規）／`run_sql_tests.sh` | 6件 |
| `supabase/schema.generated.sql` | 再生成（差分はポリシー1本） |

`database.ts` 差分なし。T6（関数の権限）は変更なし（新しい関数を作っていない）。

## 確認方法と結果

| 項目 | 結果 |
|---|---|
| 修正前 | ✅ migration 適用前に新スイートを流すと #2（本人確認前でも作成できる）と #6 が FAIL＝穴を再現 |
| SQL 全スイート | ✅ 10スイート全合格（新規6件: 確認済みは作成可／確認前は RLS 拒否／非当事者・ブロック・凍結は従来どおり拒否／ポリシーは1本で4条件を含む） |
| REST | ✅ seed13 を一時的に本人確認前にして `POST /rest/v1/calls` → **403 42501**、確認済みに戻すと **201** |
| tsc・biome | ✅ tsc 3パッケージ exit 0／biome エラー0 |

## 前提

- REST で作った calls 1行と、それに伴う user_events（call_logged）1行は削除し、calls・user_events とも計測前の件数に戻した。seed13 は本人確認済みに戻した。

## 翌朝オーナーに見てほしい点

- 正規の通話（発信者は必ず本人確認済み）には影響しない。相手の本人確認は通話トークン側（agora-token）で既に必須。
