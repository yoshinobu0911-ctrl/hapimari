# 2026-09-09 決定: 写真AIモデレーション＋いいね暴言フィルタをオーナー承認・実装

## 決定内容

1. **写真AIモデレーション**（`docs/design/photo_ai_moderation_proposal.md`）: プロバイダはA案（OpenAI omni-moderation）で承認。
2. **いいね一言メッセージの暴言・脅迫フィルタ**（`docs/design/abuse_word_filter_proposal.md`）: 提案どおり実装してよいとオーナー承認。加えて以下2点の追加指示があった。
   - 「殺すぞ」「死ね」等の**生命・身体への脅迫語は必ず遮断**すること（辞書の最優先カテゴリとして明記）。
   - 固定辞書に加えて、**AIが一言メッセージの暴力性を判定する多層防御**を追加すること（写真AIと同じOpenAI moderationsを流用し、text入力で暴力・脅迫カテゴリを検知）。

## 実装（コミット時点でコード完了・実キー未設定のため未検証）

- `packages/shared/src/abuse_words.ts` 新設。生命・身体への脅迫語を最優先カテゴリとして収録（殺すぞ・死ね 等）＋容姿/婚歴/性的強要/存在否定の4カテゴリ。`fraud_words.ts` と同じ正規化方式を共用。
- `supabase/functions/like/index.ts`: 詐欺ワード検査の直後に暴言辞書検査 → AI判定（`MESSAGE_MODERATION_API_KEY` 未設定時はスキップ、API障害時はフェイルオープンで固定辞書のみに戻る＝いいね機能自体は止めない）を追加。
- `apps/admin/lib/photo-ai.ts`: `analyzePhoto` を実装。OpenAI moderations（`omni-moderation-latest`・image_url入力）を呼び、`flagged` を ok/ng に変換。判定は常に「参考」で、承認/却下の最終操作は人力のまま。
- `apps/admin/app/photos/page.tsx`: 審査キューの初回表示時に未判定写真だけAI判定を実行し `photo_reviews.ai_verdict/ai_detail` に保存（2回目以降はAPIを呼ばない）。
- `.env.example`（admin・supabase/functions）にキーの追加箇所を明記。

## 未実施・要フォロー

- **実キー未設定のため、実際のAPI呼び出しは未検証**。`apps/admin/.env` に `PHOTO_MODERATION_API_KEY`、`supabase/functions/.env` に `MESSAGE_MODERATION_API_KEY`（いずれもOpenAIの同一キーを使い回して問題ない）を設定後、動作確認が必要。
- 暴言辞書の語彙は今回Claudeがたたき台として作成（既存の`fraud_words.ts`と同じ形式）。オーナーによる語彙の最終確認は未実施。
- プライバシーポリシーへの「第三者提供」記載要否は未確認（行政書士面談と合わせて確認予定）。

## 検証状況（このセッションで確認済み）
- `pnpm --filter @hapimari/shared test`: 93件 PASS（新規 abuse_words.test.ts 5件含む）
- `tsc --noEmit`: shared・admin ともに 0件
- `biome check`: 変更ファイルすべて 0件
- Edge Function（Deno）の型チェックは、ローカルにDeno CLI未インストールのため**未実施**
- 実際のOpenAI API呼び出し・画面での目視確認は**未実施**（実キー未設定のため）
