# 受け入れ記録: PR #1 レビュー指摘10件の修正（2026-09-02）

- 対象コミット: `d1fbcb7`（コード）
- 実施: Claude（Fable 5）／ ローカルSupabase（supabase_db_hapimari）で全件実測
- 関連文書:
  - 全指摘の棚卸し表: `docs/review/2026-09-02_指摘棚卸し.md`
  - レビュアー返信文ドラフト（未投稿）: `docs/review/2026-09-02_レビュアー返信文.md`

## 0. 第三者レビュアー向けの読み方（レビュー導線）

| 見たいもの | 場所 |
|---|---|
| **DBの最終状態（1ファイル）** | `supabase/schema.generated.sql`（public+storage。関数ACL・RLSポリシー・GRANT込み。migration適用後に `pnpm exec supabase db dump --local -s public -s storage -f supabase/schema.generated.sql` で再生成） |
| 今回の変更（DB） | `supabase/migrations/20260902100000_review2_security_fixes.sql`（指摘番号ごとにセクション分け・各節の冒頭に攻撃シナリオを記載） |
| 今回の変更（アプリ） | `apps/admin/middleware.ts` / `apps/admin/lib/admin-auth.ts` / `apps/admin/app/photos/actions.ts` / `apps/mobile/src/lib/location.ts` |
| **攻撃再現テストの一括実行** | `bash scripts/run_sql_tests.sh`（4スイート・PASS/FAILのみ表示・DBは汚さない） |
| 並列競合の実測 | `bash scripts/test_review2_concurrency.sh`（2セッション実測・テストデータは自動削除） |
| 判定の全件表 | `docs/review/2026-09-02_指摘棚卸し.md`（39コメント全件に判定+根拠） |

### 指摘 ⇔ 修正 ⇔ テストの対応表

| # | 指摘 | 修正箇所 | 検証 |
|---|---|---|---|
| 1 | review_verification がPUBLIC実行可 | migration #1節 | T1・T2 |
| 2 | 他人の写真でなりすまし | migration #2節（トリガ+is_photo_of_profile） | T3・T4 |
| 3 | 承認済みパスへの上書き | migration #3節 + photos/actions.ts | T5（4項目） |
| 4 | 位置更新制限の並列回避 | migration #4節（for update） | 並列C1 |
| 5 | is_blocked_between のrevoke漏れ | migration 横展開節 | T6 |
| 6 | voice_profile_url が書き込み可 | migration #6節（列revoke） | T7 |
| 7 | admin防御がNODE_ENV依存 | middleware.ts / admin-auth.ts | tsc・lint・コードレビュー |
| 8 | too_frequentで距離ソート消失 | location.ts | tsc・lint・コードレビュー |
| 9 | p_area の検証なし | migration #9節（+labelにも） | T8（4項目） |
| 10 | 同時いいねでマッチ欠落 | migration #10節（advisory lock） | 並列C2 + T9（直列回帰） |

## 1. 10件の結果サマリ

10件すべて「実コードで裏取り→未対応と確認→修正→攻撃再現テストで実証」を実施。
**既に対応済みだったものは0件**（依頼文の記載どおり全件が未対応だった）。

## 2. フェーズ2（敵対的検証）で新たに見つけた問題と対処

| 発見 | 対処 |
|---|---|
| **#3の迂回**: UPDATEポリシーを消しても「承認→DELETE→同一パス再アップロード」で審査済みのまま差し替え可能 | `photos_本人のみ削除可` ポリシーも撤去（T5dで実証: DELETE=permission denied・再挿入=一意制約違反） |
| **#1/#5と同型の横展開**: 既定PUBLIC実行のままの関数が多数（is_match_participant・トリガ関数群・_email_hash 等18関数） | 全関数からPUBLIC/anonのEXECUTEを機械的に剥奪し、RLSポリシー/ビューが必要とする2関数（is_blocked_between・is_match_participant）のみ authenticated へ明示grant。既定権限も変更（T6: anon実行可能関数=0件） |
| **#2と同型**: 未使用の承認照会オラクル `is_photo_approved` / `get_approved_photo_paths` が任意パスの承認有無を漏らす | authenticated から revoke（service_roleのみ）。既存テスト test_m65 を現行設計の検証経路に追随修正 |
| **#9と同型**: propose_date_slot の label も無検査で相手への自動メッセージに載る | p_area と同じ fraud_words 検査を適用（T8で実証） |
| **#1の副次**: 4引数化の際にM2版の行ロック・再審査ガードが消失していた（二重審査の競合） | for update と already_reviewed ガードを復元 |
| 既存テストの陳腐化: test_m66 が「未課金」を旧 `profiles.subscription_active` でしか作っておらず、M7.1以降は実態と乖離（FAILを見逃す状態） | subscriptions 行の削除を追加し「未課金男性の送信拒否(not_entitled)」がPASSに戻ることを確認 |
| 「読んでから書く」他経路の点検 | set_date_intent 等のデートRPC: 行更新の直列化で整合（初回同時intentのみ片方がユニーク制約エラー→再操作で解消・実害小のため様子見）。like Edge Functionの重複チェック: ユニーク制約が最終防衛で問題なし |
| 【要オーナー判断・未実施】ローカルDBに「匿名化済みなのに status='active'」の残骸行（age=126）が1件あり、test_m65 のage検査がこの行のせいでFAILする | データ修正のため未実施（§5参照） |
| 【観察・低優先】anon/authenticated に各テーブルの MAINTAIN 権限（PG17の既定）が残る | PostgREST から呼ぶ経路が無く実害なし。次回のハードニングで revoke 候補 |

## 3. 攻撃再現テストの実行結果（出力そのまま）

### 3-1. SQLスイート（`bash scripts/run_sql_tests.sh` 2026-09-02実行・抜粋は改変なし）

test_review2_fixes（今回の20項目・全PASS）:

```
=== T1: 一般利用者は review_verification を実行できない（#1） ===
NOTICE:  PASS: review_verification は permission denied (permission denied for function review_verification)
=== T2: review_verification のACLと旧3引数版の削除（#1） ===
 PASS: 旧3引数版は削除済み
 PASS: 実行権限は service_role のみ
=== T3: 他人の承認済みパスを自分の photo_urls に入れられない（#2 書き込み側） ===
NOTICE:  PASS: 書き込みが invalid_photo_path で拒否された
=== T4: 仮に書き込めても他人の写真は表示されない（#2 表示側・所有者検証） ===
 PASS: 攻撃者のプロフィールに被害者の写真は表示されない
 PASS: 持ち主本人のプロフィールでは表示される（回帰なし）
=== T5: 承認済みパスへの上書き経路が閉じている（#3） ===
 PASS: photos の UPDATE ポリシーは削除済み
NOTICE:  PASS: 上書きは0行（ポリシー不在で不可視）
 PASS: 再登録で pending に戻る（承認の持ち越し不可）
NOTICE:  PASS: 削除は permission denied
NOTICE:  PASS: 既存パスへの再挿入は一意制約違反で拒否
=== T6: 全関数ACLの総点検（#5 と同型の横展開） ===
 PASS: anon が実行できる関数は0件
=== T7: voice_profile_url は書き込み不可（#6） ===
NOTICE:  PASS: voice_profile_url の更新は permission denied
=== T8: デート提案の p_area / label 検証（#9） ===
NOTICE:  PASS: NGワード入りエリア名は拒否 (fraud_words_in_proposal)
NOTICE:  PASS: 41文字のエリア名は拒否 (invalid_area)
NOTICE:  PASS: NGワード入りラベルは拒否 (fraud_words_in_proposal)
NOTICE:  PASS: 正常な提案は成立する（回帰なし）
=== T9: 相互いいね→マッチ成立の回帰確認（#10・直列実行） ===
 PASS: 直列の相互いいねでマッチ1件成立
=== T10: 回帰確認（一覧・距離・審査状況の主要経路） ===
 PASS: profiles_public で他人が見える (21行)
 PASS: get_profile_distances が実行できる
 PASS: photo_reviews の自分の行を閲覧できる
```

T6の付帯出力: authenticated が実行できる関数は次の19件のみ（すべて意図した公開RPCとRLS/ビュー用ヘルパ）:
can_caller_message / cancel_date / get_date_status / get_profile_distances / is_blocked_between /
is_caller_active / is_match_blocked / is_match_participant / is_photo_of_profile / is_photo_visible_to /
is_subscription_active / log_user_event / propose_date_slot / register_photo_for_review /
respond_date_slot / set_date_intent / set_my_location / submit_date_feedback / withdraw_account

### 3-2. 並列競合（`bash scripts/test_review2_concurrency.sh` 2026-09-02実行・出力そのまま）

```
=== C1: 位置更新の並列リクエスト（#4） ===
--- セッションA出力 ---
A: committed
--- セッションB出力（所要 2487ms） ---
ERROR:  too_frequent
CONTEXT:  PL/pgSQL function set_my_location(double precision,double precision) line 16 at RAISE
PASS: 後発はロック解放を待った上で too_frequent（並列でも1回しか通らない）
最終状態 daily_count/updated_at: 2/04:45:05（daily_countが1回分だけ増えて2なら正常）

=== C2: 相互いいねの同時挿入（#10） ===
--- セッションA出力 ---
A: committed
--- セッションB出力（所要 2517ms） ---
B: inserted
マッチ件数: 1
PASS: 後発がロックで直列化され、同時相互いいねでもマッチが1件成立
（テストデータは削除済み）
```

（後発セッションの所要 約2.5秒 ＝ 先発のコミットまでロック待ちした実証。修正前は即時に通過/欠落していた）

## 4. 回帰確認（実出力）

- `pnpm test`: **Test Files 11 passed (11) / Tests 88 passed (88)**
- `pnpm lint`（biome check）: **Checked 140 files. No fixes applied.**（エラー0。middleware.tsのフォーマット1件はbiome format --writeで解消済み）
- `apps/mobile` `pnpm exec tsc --noEmit`: **exit 0**
- `apps/admin` `pnpm exec tsc --noEmit`: **exit 0**
- 既存SQLスイート: test_m66_audit_fixes 全PASS（陳腐化1箇所を追随修正後）・test_m67_retention 全24項目PASS・test_m65_p1 は**残FAIL 1件のみ**（下記§5のローカル残骸データ起因。コード起因ではない）

## 5. 未完了・オーナー判断待ち

1. **ローカルDBの残骸データ修正（承認待ち）**: `status='active'` のまま匿名化された行が1件あり（nickname='退会済み'・birth_date=1900年・過去の手動テストの残骸）、test_m65 のage検査がFAILする。修正SQL（1行・ローカルのみ）:
   `update profiles set status = 'withdrawn', withdrawn_at = coalesce(withdrawn_at, anonymized_at) where anonymized_at is not null and status = 'active';`
   → データ上書きにあたるため実行前にオーナー承認を求める（QUESTIONS.md Q7）。
2. **`import 'server-only'`（P3）**: `server-only` の依存追加が必要なため見送り（QUESTIONS.md Q6）。
3. **スキーマsnapshot/型のCI同期（P3）**: snapshot生成は実施済み。CI組み込みは③-16（CI整備時）。
4. GitHubへの返信投稿は**未実施**（ドラフトのみ。オーナー確認後に投稿）。

## 6. このレビューの位置づけ（憲法§9）

- 本変更は **(C) 本番公開前レビュー**の対象（RLS・権限・写真・管理画面のセンシティブ領域）。
- 専門家に必ず見てほしい点:
  - migration「横展開」節の**全関数ACL一括revoke**が、将来の関数追加運用（明示grant必須化）と整合しているか
  - `_validate_photo_ownership` トリガの例外系（外部URL許容の妥当性。表示側の所有者検証があるため無害という判断）
  - photosバケットのUPDATE/DELETEポリシー撤去がプロダクト要件（利用者による写真削除）と衝突しないか
