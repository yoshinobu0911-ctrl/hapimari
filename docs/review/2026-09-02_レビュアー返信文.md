# PR #1 インラインコメントへの返信文ドラフト（2026-09-02）

> **これはドラフトです。GitHubには未投稿**。オーナーが内容確認のうえ、各コメントIDのスレッドに投稿してください。
> 対象: satoman0703さんの2回目レビュー（7/25・8件）と、8/27返信で「対応済み」と
> 書いてしまったが実際は未対応だった4件（boooleonardoさん 7/18分）。
> 修正はすべてコミット `d1fbcb7` に含まれます。
> 検証の再現: `bash scripts/run_sql_tests.sh`（攻撃再現20件）/ `bash scripts/test_review2_concurrency.sh`（並列2件）

---

## satoman0703さん 2回目レビュー（7/25）への返信

### → コメント 3651265977（review_verification のACL）

ご指摘のとおりでした。引数追加で `create or replace` にならず、**ACLなしの4引数版が別関数として作られ、利用者が自分の本人確認を承認できる状態**でした。`d1fbcb7` で対応しました（`supabase/migrations/20260902100000_review2_security_fixes.sql` #1節）。

- ご提示のSQLどおり、旧3引数版を drop し、4引数版を revoke → service_role のみに grant
- 再発防止の `alter default privileges ... revoke execute on functions from public` も適用
- あわせて、4引数化の際に消えていた **行ロック（for update）と再審査ガード**（M2版にはあった）も復元
- さらに横展開として全関数のACLを機械的に総点検し、`is_blocked_between` や `is_match_participant` など**既定PUBLIC実行のまま残っていた関数をすべて剥奪**（必要な関数のみ authenticated へ明示grant）

検証: `scripts/test_review2_fixes.sql` T1（authenticated から実行→permission denied）・T2（旧版削除とACL）・T6（**anonが実行できる関数が0件**であることの機械的確認）がPASS。スキーマの最終状態は今回から `supabase/schema.generated.sql` で一覧できます。

### → コメント 3651266310（set_my_location の行ロック）

ご指摘のとおり、`select into` だけでは並列リクエストが両方とも制限を通過できました。ご提示どおり `for update` を追加しました（`d1fbcb7`・同migration #4節）。

検証（実測）: `scripts/test_review2_concurrency.sh` C1 — 2時間前更新済みの行に対しセッションAが更新を保持したままの状態でセッションBが並列更新 → **Bは2,487ms待機したうえで too_frequent で拒否**され、daily_count は1回分しか増えないことを確認しました。

### → コメント 3651266642（写真の所有者検証）

ご指摘のとおり、パスだけで判定しており所有者を見ていませんでした。`d1fbcb7` で書き込み側・表示側の両方を塞ぎました（同migration #2節）。

- **書き込み側（根本対応）**: `photo_urls` への insert/update に security definer トリガを追加し、自分のフォルダ配下（`auth.uid()/`）以外のパスは `invalid_photo_path` で拒否。攻撃がここで失敗するため、運営検知の問題も解消されます
- **表示側**: `is_photo_of_profile(path, owner)` を新設し、`profiles_public` は「**photo_reviews.user_id がそのプロフィールの持ち主と一致する承認済み写真**」だけを返すよう変更（未知の書込経路が今後できても表示されない）

検証: T3（他人パスの書き込み→invalid_photo_path）・T4（postgres権限で攻撃状態を直接作っても第三者からは photo_urls が空/持ち主本人の表示は回帰なし）がPASS。

### → コメント 3651266959（承認済みパスへの上書き）

ご指摘のとおりでした。`d1fbcb7` で3段構えにしました（同migration #3節）。

- `photos_本人のみ更新可` ポリシーを撤去（クライアントは常に新パス `photo_<epoch>` でuploadするため機能影響なし）
- 敵対的に自己検証したところ「**承認取得→DELETE→同一パスへ再アップロード**」という迂回が残っていたため、`photos_本人のみ削除可` も撤去（クライアントに削除機能はなく、退会時の削除は retention ジョブ=service_role が実施）
- 多層防御として `register_photo_for_review` の再登録時は必ず `pending` へ戻す（従来は on conflict do nothing で approved が残存）
- `rejectPhoto` は**オブジェクトの実体も削除**するよう修正（`apps/admin/app/photos/actions.ts:37-42`）。既発行の署名URLも実体消滅により即無効化されます

検証: T5の4項目（UPDATEポリシー不在・上書き0行・再登録でpending・DELETE拒否+再挿入は一意制約違反）がPASS。

### → コメント 3651267326（admin防御のNODE_ENV依存）

ご提示の方式で対応しました（`d1fbcb7`・`apps/admin/middleware.ts`）。

- `ADMIN_PASSWORD` 未設定時は**環境を問わず503**。素通しは `ADMIN_ALLOW_INSECURE=1` かつ非production の両方が揃った場合のみ
- 本番で `x-forwarded-proto` が https 以外なら403、本番レスポンスにHSTS付与
- Server Action 側の二重チェック（`apps/admin/lib/admin-auth.ts`）も同じ方針に統一

### → コメント 3651267595（import 'server-only'）

ご提案に同意ですが、`server-only` パッケージが未導入で**依存追加はオーナー承認が必要**な運用のため、QUESTIONS.md に記録して承認待ちにしています。承認され次第1行で追加します。

### → コメント 3651267831（too_frequent で距離ソート消失）

ご指摘のとおりの利用者影響（30分以内の開き直しで距離ソート不可）を確認しました。ご提示のコードのまま採用しています（`d1fbcb7`・`apps/mobile/src/lib/location.ts:53-58`）。too_frequent / daily_limit は「座標保存済み＝距離機能は使える」として true を返します。

### → コメント 3651268192（スキーマのスナップショット）

`supabase db dump --local -s public -s storage` の生成物を `supabase/schema.generated.sql` としてコミットしました（関数ACL・RLSポリシー・GRANTを含む最終状態が1ファイルで読めます）。migration適用後に再生成する運用にします。**CIでの同期保証はCI基盤の整備時（launch_checklist ③-16）にまとめて導入**とさせてください。

---

## 8/27返信の訂正（boooleonardoさん 7/18分・4件）

> 8/27の返信で「対応済み」とご報告した中に、実際は未対応のものが4件ありました。
> お詫びして訂正します。いずれも `d1fbcb7` で対応済みです。

### → コメント 3608467168（is_blocked_between の revoke漏れ）

8/27に「対応済み」とお伝えしましたが、**実際にはACLが無くPUBLIC実行可のまま**でした。`d1fbcb7` で revoke し、authenticated / service_role のみに明示grantしました。さらに同型の漏れを機械的に総点検し、**public スキーマの全58関数のうち anon が実行できる関数を0件**にしました（`scripts/test_review2_fixes.sql` T6で機械的に検証）。

### → コメント 3608471433（同時likeでマッチ欠落）

こちらも実際は未対応でした。`create_match_on_mutual_like` にペア単位のアドバイザリロック（`pg_advisory_xact_lock`）を追加し、同時挿入を直列化しました（`d1fbcb7`）。実測: 先行トランザクションが未コミットの状態で逆方向のlikeを並列挿入 → **後発が2,517ms待機したのちマッチが確実に1件成立**（`scripts/test_review2_concurrency.sh` C2）。

### → コメント 3608476311（p_area の検証）

こちらも実際は未対応でした。`propose_date_slot` に p_area の40文字制限と fraud_words 検査（いいねの一言と同じ辞書・同じ正規化）を追加しました（`d1fbcb7`）。あわせて**同じ経路で相手に届く slot の label にも**同じ検査を適用しています。検証: T8の4項目（NGワードエリア名拒否・41文字拒否・NGワードラベル拒否・正常系は成立）PASS。

### → コメント 3608493699（voice_profile_url）

写真側はパス保持+署名URLに移行済みでしたが、**音声プロフィールのURLは文字列のまま書き込み可能で未対応**でした。音声機能自体が未実装（アプリに読み書きコードが存在しない）ため、`d1fbcb7` で列の insert/update 権限を revoke し、機能実装まで封鎖しました。実装時は写真と同様「バケット内パス+審査」方式にします。検証: T7（任意URLの保存→permission denied）PASS。
