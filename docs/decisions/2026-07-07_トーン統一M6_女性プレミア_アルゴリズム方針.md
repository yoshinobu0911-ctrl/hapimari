# 2026-07-07 オーナー回答の記録（トーン統一・課金将来枠・アルゴリズム方針）

2026-07-06 の「ターゲット再確認（中高年45〜65）」に伴う確認質問3点への回答と、追加指示の記録。

## 1. 子持ち表示について

**オーナー回答**: 「子持ちかどうかは書かなくていいと思います」

- 解釈: プロフィール上で子持ちかどうかを前面に出す必要はない（中高年アプリとしての見せ方）。
- **具体的な適用範囲は M6 トーン統一の設計提案で選択肢を出して確定する**:
  (a) 詳細画面の表示のみ廃止 (b) オンボーディングの入力自体を任意化/廃止
  (c) それに伴う R3（子持ち理解ゲート）の存廃・緩和
- 注意: R3 を廃止する場合、SPEC §4 R3 と M3受け入れ条件2の仕様変更になるため、
  M6設計時に影響（Edge Function like・like_rules.ts・Vitest・受け入れ記録）を明示して承認を得る。
- それまでは現行実装（M3時点の仕様）を維持する。

## 2. トーン統一（中高年45〜65向けの文言・seed・タグ見直し）

**オーナー回答**: 「M6の仕上げでまとめてください」→ M6 のスコープに組み込む。

## 3. 女性プレミアム課金（500円/月）

**オーナー回答**: 「課金モックの設計に将来枠として入れてほしい」

- **理由（オーナーの言葉）**: まじめな相手を探している女性に見限られないようにするため。
  婚活は本来男女とも課金するモデルであり、男性だけ課金のアプリだと真剣でない男性が混ざる。
  女性側にも少額課金の枠を用意することで、真剣度の低い層を除外する狙い。
- 対応: M6 の `PaymentProvider` / プラン定義に「女性プレミアム（500円/月・機能は後決め）」の
  インターフェースだけ用意する（UI露出はMVPではしない）。

## 4. マッチングアルゴリズムの追加開発（追加指示）

**オーナー指示**: 価値観マッチングを押し出している with のようにしたい。
ただし中高年ユーザーにアプリ上でMBTI的な診断をさせるのは親和性が悪いという認識。

- 方針（エージェント提案・オーナー未承認の叩き台）:
  - **診断テストは作らない**。代わりに (a) 相性の「理由」の言語化表示（「お二人とも◯◯派」等）
    (b) オンボーディングの価値観タグ選択を「質問に答える形式」に見せ替え（データは同じタグ）
    (c) タグの拡充・カテゴリ再編（中高年向け）を M6 トーン統一と同時に実施
  - **行動データを使う本格推薦（いいね通過率・返信率の学習）は公開後の M7** として別途設計
    （学習材料となる実ユーザーの行動ログが必要なため）。
- 決定時期: (a)(b)(c) は M6 設計時に具体案を提示して承認を得る。M7 は公開後に改めて提案。

## 次のステップ

オーナー指示「上記が解決したら次のステップ（M4？M5？）に進んでください」
→ マイルストーン順どおり **M4（デート移行支援）** へ。CLAUDE.md §4 に従い
`docs/design/M4_design.md` を作成し、設計承認後に実装を開始する。

## 追記（2026-07-07）

M4設計書（docs/design/M4_design.md）の設計判断4点（自動メッセージ方式・今はまだの完全秘匿・確定後キャンセルあり・フィードバック非開示）を**推奨案どおりオーナー承認（「OK」）**。M4実装を開始する。

## 追記2（2026-07-07）

M5設計書（docs/design/M5_design.md）の設計判断4点（音声なしモック・calls RLS強化・15分切断は単体テスト担保・着信はチャット画面表示中のみ）を**推奨案どおりオーナー承認**。M5実装を開始する。
