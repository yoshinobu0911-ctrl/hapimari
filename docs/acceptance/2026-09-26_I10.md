# 受け入れ記録: I10 いいねの本人確認ゲートを validateLike に統合（2026-09-26 夜間作業）

- 実装: Claude Opus 5.5（夜間自律作業・モードA。オーナー指示 2026-09-25 00:55 で Opus もモードA全範囲を実装可）
- 設計: [P2 必須6件 設計提案](../design/2026-09-24_P2必須6件_設計提案.md) の I10 節（判断が分かれる点はすべて推奨案＝1:A／2:A／3:B）
- 人間レビューの段階: (B) MVP 完成時。リスク低（挙動変更なし）

## 対象

| ファイル | 内容 |
|---|---|
| `packages/shared/src/like_rules.ts` | `isVerified` 必須フィールド・`not_verified` エラー・`validateLikeSender()` を追加。`validateLike` の先頭で呼ぶ |
| `supabase/functions/like/index.ts` | 直書きの `is_verified !== true` 判定を `validateLikeSender()` 呼び出しへ置換（位置は相手取得より前のまま）。`toRuleUser` に `isVerified` を追加 |
| `packages/shared/test/like_rules.test.ts` | ヘルパに `isVerified: true`、テスト4件追加 |

## 確認方法と結果

| # | 受け入れ条件 | 結果 |
|---|---|---|
| 1 | shared vitest 全件成功・+4件 | ✅ 93 → **97件** 成功。既存6件の本体は無変更 |
| 2 | typecheck / biome | ✅ tsc shared・mobile・admin すべて exit 0 ／ biome 142ファイル エラー0 |
| 3 | grep | ✅ `is_verified !== true` 0件／`isVerified: p.is_verified === true` 1件（86行）／`validateLikeSender(` 192行 < `.eq('id', toUser)` 204行 |
| 4 | 壊す確認 | ✅ 本人確認判定を status 判定の後ろへ移すと「本人確認未了は他のどの拒否理由より先に判定される」が赤（1 failed / 96 passed）。元に戻して 97 passed |
| 5・6 | HTTP 実測（実装前後で同一） | ✅ 下表の8ケースで**実装前と実装後の status と JSON 本文が完全一致**（diff 無し） |
| 7 | 直接 INSERT / is_verified 自己書換の不可 | ✅ authenticated で `insert into likes` → `permission denied for table likes`、`update profiles set is_verified=true` → `permission denied for table profiles` |
| 8 | I29 後の回帰 | 未（I29 実装時に再実測する） |

HTTP 実測（ローカル `supabase functions serve`、006 は一時的に is_verified=false、計測後に元へ戻し likes/user_events の計測行も削除）:

| ケース | 応答（前後とも） |
|---|---|
| 未確認006→018 | 403 not_verified |
| 未確認006→自分 | 403 not_verified |
| 未確認006→"abc" | 403 not_verified |
| 未確認006→016（いいね済み） | 403 not_verified |
| 未確認006→018・一言「投資の話」 | 400 fraud_message（本人確認より前の判定は不変） |
| 確認済み004→自分 | 400 self_like |
| 確認済み004→存在しないUUID | 404 target_not_found |
| 確認済み004→018 | 200 `{"ok":true,"matched":false,"carriedOver":false}` |

## 前提

- **HTTP 実測は `packages/shared/src/abuse_words.ts:8` の import を一時的に `./fraud_words.ts` に書き換えた状態で行った**（書き換えないと Edge Runtime が like 関数を読み込めず、`functions serve` 自体が起動しない。下記「追加の発見」）。この一時変更はコミットしておらず、計測後に `git restore` で戻した。
- Git Bash から curl に日本語を引数で渡すと文字化けし詐欺語判定を通過してしまうため、日本語を含む本文は UTF-8 ファイルから送った（初回の誤計測は破棄）。
- DB の一時変更（006 の is_verified、004→018 のいいね、like_sent イベント）はすべて元に戻した。

## 未検証の項目

- Deno による型検査（deno 未導入）。
- I29 適用後の回帰（受け入れ条件8）。

## 翌朝オーナーに見てほしい点

- **（重要・既存不具合）`like` 関数は現状の main のままでは Edge Runtime で起動しない。** `abuse_words.ts`（2026-09-20 `2a7c842` で追加）が `./fraud_words` を拡張子なしで import しており、Deno が解決できない。ローカルで `supabase functions serve` が全関数まとめて失敗する。本番に `functions deploy like` しても同じ理由で失敗するか、いいねが全件エラーになる見込み（本番デプロイは未実施のため未確認）。修正方針の選択は夜間作業報告の確認事項に記載。
