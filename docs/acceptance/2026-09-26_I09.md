# 受け入れ記録: I09 通話失敗時のサーバー案内を表示し、相手が本人確認前なら通話ボタンを隠す（2026-09-26 夜間作業）

- 実装: Claude Opus 5.5（夜間自律作業・モードA）
- 設計: [P2 必須6件 設計提案](../design/2026-09-24_P2必須6件_設計提案.md) の I09 節（判断①〜⑤はすべて推奨A）
- 人間レビューの段階: (B) MVP 完成時。リスク高（本人確認に関わる表示）。サーバーの判定は変更していない

## 対象

| ファイル | 内容 |
|---|---|
| `packages/shared/src/call-provider.ts` | `CallEndDetail` 型、`onEnded` の省略可能な第2引数、純粋関数 `callSetupFailureMessage()` |
| `packages/shared/test/call_provider.test.ts` | `callSetupFailureMessage` のテスト2件（規約どおり2例／規約外8例） |
| `apps/mobile/src/lib/call-provider-agora.ts` | 失敗応答の本文から案内文を取り出し `CallSetupError`→`onEnded` で画面へ運ぶ。timeout 等で理由を上書きしたときは detail を捨てる |
| `apps/mobile/src/app/call/[matchId].tsx` | `endMessage` を優先表示。案内表示中は自動で戻らない。相手プロフィールのキャッシュキーを `['profile', id, 'call']` に分離 |
| `apps/mobile/src/app/chat/[matchId].tsx` | 通話ボタンの条件に「相手が本人確認済み（profiles_public.is_verified）」を追加 |

## 確認方法と結果

| # | 受け入れ条件 | 結果 |
|---|---|---|
| 1 | vitest | ✅ 97 → **99件** 成功（既存の 1800 秒境界・表示形式テストは無変更で成功） |
| 2 | tsc / biome | ✅ shared・mobile・admin exit 0 ／ biome エラー0 |
| 3 | Web SDK の隔離 | ✅ `call-provider-agora` の import は `call-provider.web.ts:5` の1件だけ／`agora-rtc-sdk-ng` を含むのは `call-provider-agora.ts` の1件だけ |
| 壊す | 恒真でないこと | ✅ `ok !== false` の条件を外すと規約外テストが赤（1 failed / 98 passed）。戻して 99 passed |
| 5〜10 相当 | サーバーの案内が取り出せる（HTTP＋クライアント処理） | ✅ 下表。アプリと同じ supabase-js（`functions.invoke`）で agora-token を呼び、`call-provider-agora.ts` と同じ手順で本文を取り出した |
| 11 相当 | 通信断では汎用文言 | ✅ agora-token だけ fetch 失敗させると `FunctionsFetchError` になり、案内文なし（画面は従来の「接続できませんでした…」） |
| 4・5・6・7・12・14 | 画面の実描画（ボタンの消失・自動で戻らない・キャッシュ取り違えなし・マイク拒否） | **未検証**（ブラウザでの実描画を行っていない） |
| 9 再 | I29 後のブロック案内 | 未（I29 実装時） |

実測（`supabase functions serve`・Agora キー未設定。match `10000000-…0001` は seed01×seed13）:

| ケース | 応答 | 画面に出る文言（取り出し結果） |
|---|---|---|
| 資格OK・キー未設定 | 500 | エラーが発生しました。時間をおいてお試しください。 |
| 非当事者（seed02） | 404 | この通話はご利用いただけません。 |
| 相手が未確認（seed13 を一時 false） | 410 | 現在おかけになれません。 |
| 自分が未確認（seed13 が発信） | 403 | 本人確認の完了後にご利用いただけます。 |
| matchId 空 | 400 | リクエストが不正です。 |
| ブロック（01→13 を一時挿入） | 403 | この通話はご利用いただけません。 |
| 通信断 | FunctionsFetchError | （汎用）接続できませんでした… |

## 前提

- Edge Runtime を起動するため、`abuse_words.ts:8` の import を一時的に `.ts` 付きにした（コミットしていない。I10 受け入れ記録の「翌朝見てほしい点」参照）。
- DB の一時変更（seed13 の is_verified、01→13 のブロック）は各計測の直後に戻し、`is_verified=false` の利用者0件・該当ブロック0件を確認した。
- 取り出し処理の実測はスクリプト内に同じ関数を写して行った（関数そのものは vitest で検査済み）。

## 未検証の項目

- ブラウザでの実描画（受け入れ条件4〜7・12・14）。特に「相手未確認でボタンが消える」「案内表示中は自動で戻らない」「通話から戻ってもボタンが消えない（キャッシュキー分離）」は、コード上の確認のみ。
- 実音声（Agora キー未設定）。

## 翌朝オーナーに見てほしい点

- 相手が本人確認前のマッチでは、チャット画面の通話ボタンが出なくなる（推奨A）。発生するのは 2026-08-26 より前のいいねを起点としたマッチだけ。
- 案内が出たときは自動でチャットへ戻らず、「チャットへ戻る」を押す必要がある（推奨A。中高年の利用者が読み切れるように）。
