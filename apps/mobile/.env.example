# Supabaseローカル環境（supabase start の出力値）
# 実機で動かす場合は 127.0.0.1 をPCのLAN IPに置き換える
EXPO_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
EXPO_PUBLIC_SUPABASE_ANON_KEY=<supabase start の anon key>

# M5（音声通話）で使用。取得までは CallProvider のモック実装で動作する
EXPO_PUBLIC_AGORA_APP_ID=<Agora APP ID（未取得のためプレースホルダ）>
