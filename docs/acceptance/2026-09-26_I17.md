# 受け入れ記録: I17 Edge Function 共通の HTTP・認証ヘルパを `_shared/http.ts` に一本化（2026-09-26 夜間作業）

- 実装: Claude Opus 5.5（夜間自律作業。P4 は 2026-09-24 包括承認「作業側が進める」）
- 方針: 統合表 I17「P4 として分離、今回の決済修正についでの整理を混ぜない」、修正指示書 #17「統合時にどちら（authenticate の戻り値）に寄せるか明示」
- 人間レビューの段階: (B) MVP 完成時。決済・通話・いいねの関数が読み込む共通部分（挙動は変えない）

## 設計（包括承認のため受け入れ記録に要約）

| 項目 | 内容 |
|---|---|
| 目的 | `corsHeaders`・`json`・`fail`・`internalError`・`requiredEnv`・`adminClient`・`authenticate` が `_shared/stripe.ts` と `agora-token` に二重、`corsHeaders`・`json` が `like` にも重複していた |
| 変更範囲（S） | 新規 `supabase/functions/_shared/http.ts`（Stripe SDK を読み込まない）。`_shared/stripe.ts` は汎用部分を削除して http.ts から**再公開**（stripe-checkout/cancel/webhook の import 文は無変更）。`agora-token` は自前の汎用ヘルパを削除して http.ts から import。`like` は `corsHeaders`・`json` だけ http.ts から import |
| authenticate の統合先 | **`{ id, email }` を返す _shared/stripe.ts 版に寄せた**（stripe-checkout が email を使うため。agora-token は id だけ使うので影響なし） |
| 決済ロジック | 一切変更していない（stripe.ts の Stripe 固有部分・stripe-* 本体は無変更） |

## 確認方法と結果

| 項目 | 結果 |
|---|---|
| 5関数の基本応答の前後比較（ローカル `functions serve`、ダミー Agora キー） | ✅ like・agora-token・stripe-checkout・stripe-cancel・stripe-webhook の OPTIONS（CORS ヘッダ）・GET・非 JSON の POST・`{}` の POST・偽 JWT の POST が、**変更前と変更後で完全一致**（diff なし） |
| 正常系 | ✅ agora-token 200（トークン発行）、like 200（004→018）・403 not_verified・400 fraud_message（I10 の計測と同じ） |
| vitest・tsc・biome | ✅ vitest 121／tsc shared・mobile・admin exit 0／biome 149ファイル エラー0 |
| Edge Function の型検査 | 未（deno 未導入。実行時に全関数が起動・応答することで代替確認） |

## 前提

- Edge Runtime の起動には `abuse_words.ts:8` の import を一時的に `.ts` 付きにした（コミットしていない。既存不具合として報告書に記載）。
- 計測で入った DB の変更（004→018 のいいね・イベント、006 の is_verified）は戻した。

## 翌朝オーナーに見てほしい点

- 挙動の変化は無い。決済修正（決済レビューの must 4件）を今後行うときは、共通部分が `_shared/http.ts` にあることに注意（stripe.ts からの import はそのまま使える）。
