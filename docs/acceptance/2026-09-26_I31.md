# 受け入れ記録: I31 古い受け入れ条件と公開手順の訂正（2026-09-26 夜間作業）

- 実装: Claude Opus 5.5（夜間自律作業。P4 は 2026-09-24 包括承認「作業側が進める」）
- 方針: 統合表 I31（Codex R15・Grok#11 の残り）。旧提案 R15 の推奨A「SPEC の旧マイルストーンは歴史的計画と明記し、現在の条件を併記。SPEC 全体は書き直さない」
- 文書のみ（コード・DB 変更なし）

## 変更

| ファイル | 内容 |
|---|---|
| `SPEC.md` §6 | 冒頭に「当初計画（歴史的記述）・現行のテスト条件として使わない」注記。M1（年齢: 現行は男女35歳以上、44歳男性は登録可）・M3（R3 廃止、いいねは本人確認後のみ）・M4（R5 廃止、メッセージ数の解禁なし）・M5（通話は双方の本人確認が前提、Agora 本実装済み）・M6（Stripe 本実装済み）に「現行:」を追記。旧条件の文言は残した（受け入れ記録の改ざんをしない方針） |
| `SPEC.md` §8 | 決済・通話の行に「現行: 本実装に移行済み」を追記 |
| `docs/launch_checklist.md` ②-6・②-20 | 「現在はモック」→ コード実装済み・残りは実キー疎通（決済はレビューの must 対応も） |
| `docs/release_web.md` STEP 2 | `functions deploy` に **agora-token を追加**、Functions→migration の順に入れ替え（I28→I29 の反映順・アプリ配信は DB の後）、migration 本数を更新、**Agora と MESSAGE_MODERATION_API_KEY のシークレットを追加**、like 関数の既知不具合（起動しない）の警告 |

## 確認方法

- 記載内容を実コード・決定記録と照合: R1（`SPEC.md:166`・`profiles_min_age_check` は `birth_date <= current_date - 35 years`）、R3/R5 廃止（`SPEC.md:168,170`・`docs/decisions/2026-07-12_R3撤廃_通話即時解禁.md`）、Edge Function の環境変数（`Deno.env.get` と `requiredEnv` の呼び出しを grep、`supabase/functions/.env.example` のキー一覧と一致）、migration 本数（`supabase/migrations` 30ファイル）。
- アプリのテストは文書変更だけを理由に再実行していない（旧提案 R15 の受入条件どおり）。

## 実装しなかったもの

- I38（`apps/mobile/.env.example` の Agora 行の訂正）: 夜間指示書 §3「`.env` 系ファイルの編集禁止」に `.env.example` が含まれるか判断できないため、編集せず報告書に差分案を載せた。
