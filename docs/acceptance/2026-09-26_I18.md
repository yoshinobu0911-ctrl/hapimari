# 受け入れ記録: I18 管理画面の Basic 認証の純粋処理を1か所へ（2026-09-26 夜間作業）

- 実装: Claude Opus 5.5（夜間自律作業。P4 は 2026-09-24 包括承認「作業側が進める」）
- 方針: 2026-09-24 決定「I18 は `next/headers` に依存しない純粋ヘルパへ分離（middleware への直 import はしない）」、修正指示書 #18
- 人間レビューの段階: (B) MVP 完成時。**認証コード（憲法 §6）**

## 設計（包括承認のため受け入れ記録に要約）

| 項目 | 内容 |
|---|---|
| 目的 | `middleware.ts` と `lib/admin-auth.ts` に同じ `timingSafeEqual`・Basic 解析が二重に書かれていた（統合表 I18）。片方だけ直す事故を防ぐ |
| 変更範囲（S） | 新規 `apps/admin/lib/basic-auth.ts`（`timingSafeEqual`・`isValidBasicAuth`・`allowsInsecureWithoutPassword`。import なしの純粋関数）。`middleware.ts`・`admin-auth.ts` はこれを呼ぶだけにした。テスト `packages/shared/test/admin_basic_auth.test.ts`（admin にテスト実行器が無いため、I28 と同じく shared の Vitest から相対 import） |
| 判断 | `admin-auth.ts`（next/headers 依存）を middleware から import する案は、決定どおり採らない |
| リスク | 高（認証）。判定の条件は変えていない |

## 確認方法と結果

| 項目 | 結果 |
|---|---|
| 実測（`next dev`、環境変数はコマンドラインで指定し `.env` は読まず・編集せず） | ✅ ADMIN_PASSWORD 未設定: 認証なし・誤り・正しいのすべて **503**／設定あり: 認証なし **401**・誤り **401**・正しい **200** |
| 変更前との比較（同じ手順で旧コードを計測） | 認証なし 401・誤り 401・正しい 200 は同じ。**不正な base64 の Authorization だけ 500 → 401 に変わった**（旧コードは `atob` の例外で 500 になっていた。安全側の変化） |
| vitest | ✅ 114 → **119件**（正誤・`:` を含むパスワード・不正ヘッダ・長さ違い・未設定時の許可条件） |
| tsc・biome | ✅ tsc shared・mobile・admin exit 0／biome エラー0 |
| 二重実装の解消 | ✅ `timingSafeEqual` の定義は `basic-auth.ts` の1か所だけ |

## 翌朝オーナーに見てほしい点

- 管理画面の認証の動きは変わらない（壊れた認証ヘッダが来たときにエラー画面ではなく「認証が必要です」になる点だけ変化）。
